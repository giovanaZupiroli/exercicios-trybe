let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
// 1
// for (let index = 1; index < numbers.length; index += 1) {
//     for (let second = 0; second < index; second += 1) { 
//         if (numbers[index] < numbers[second]) {
//             let posicao = numbers[index];
//             numbers[index] = numbers[second];
//             numbers[second] = posicao;
//         }
//     }
// }
// console.log(numbers);
// 2
// for (let index = 1; index < numbers.length; index += 1) {
//     for (let second = 0; second < index; second += 1) { 
//         if (numbers[index] > numbers[second]) {
//             let posicao = numbers[index];
//             numbers[index] = numbers[second];
//             numbers[second] = posicao;
//         }
//     }
// }
// console.log(numbers);

// 3
let nova = [];